import React from 'react';
import {View, Text} from 'react-native';

const Listen = () => {
  return (
    <View>
      <Text>This is the Listen screen</Text>
    </View>
  );
};

export default Listen;
